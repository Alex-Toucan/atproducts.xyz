---
const { id, scrollcontent } = Astro.props;
---

<main id={id}>
  <div class="bd-toc md:col-3 story-top-buttons">
    <div id={`${id}-list`} class="list-group story-buttons">
      {scrollcontent ? (
        <div class="p-3 pb-0 rounded-4">
        {scrollcontent.map(({ name, href, additionalClasses }, index) => (
          <a class={`list-group-item list-group-item-action ${additionalClasses}`} href={href} key={index}>
            {name}
          </a>
        ))}
        </div>
      ) : (
        <div class="md:p-3 pb-0">
          <strong class="d-none md:d-block">There are no sections to scroll to in this story/article.</strong>
        </div>
      )}      
    </div>
  </div>
  <div class="md:col-9 p-3" tabindex="0" data-bs-smooth-scroll="true" data-bs-target={`${id}-list`} data-bs-spy="scroll">
    <div class="lg:mx-auto text-justify container-story">
      <slot name="scrollspy-content" />
    </div>
  </div>
</main>
