---
const {
  id,
  title,
  description,
  buttons,
  dropdownItems 
} = Astro.props;
---

<div id="intro" class={`hero hero-${id ? id : 'ehs'} text-center mb-3`} data-bs-theme="dark">
  <div class="hero-back h-100 p-3 position-relative">
    <div class="hero-filter w-100 align-items-center d-flex justify-content-center my-466 text-center">
      <div class="text-white">
        <div>
          {title && (
            <h1 class="display-3 fw-bold">{title}</h1>
          )}
        </div>
        <p class="fs-lg m-3 text-body-secondary" set:html={description}></p>
        <div class="mx-auto justify-content-between container" id="links">
          <div class="md:d-flex d-grid gap-2 container">
            {buttons && (
              buttons.map(({ href, text, type }, index) => (
                <a href={href} class={`btn ${type === 'primary' ? 'btn-solid theme-primary' : 'btn-solid theme-secondary'} md:col`} set:html={text}></a>
              ))
            )}
            {dropdownItems && (
              <div class="dropdown md:col">
                <button aria-expanded="false" data-bs-toggle="menu" type="button" class="btn-solid theme-secondary dropdown-toggle w-100">
                  Navigation
                </button>
                <ul class="menu w-100 white-space-normal">
                  {dropdownItems.map(({ navigationDesc, navigationLink }, index) => (
                    <li>
                      <a class="menu-item" href={navigationLink}>{navigationDesc}</a>
                    </li>
                  ))}
                </ul>
              </div>
            )}
          </div>
          <hr class="mx-4 md:mx-0 opacity-75">
          <div class="md:d-flex mt-3 justify-content-between container" id="links">
            <h2 class="md:me-auto text-centertostart mb-0">Contact Us</h2>
            <br class="disbr">
            <div class="d-flex justify-content-center gap-3">
              <a href="https://www.instagram.com/void.iso" aria-label="Instagram" target="_blank" rel="noopener noreferrer" class="link-hero fs-2xl" data-bs-toggle="tooltip" data-bs-placement="bottom" data-bs-title="Instagram"><i class="bi bi-instagram"></i></a>
              <a href="/form" aria-label="Our Support Email" target="_blank" rel="noopener noreferrer" class="link-hero fs-2xl" data-bs-toggle="tooltip" data-bs-placement="bottom" data-bs-title="Our Support Email"><i class="bi bi-envelope-fill"></i></a>  
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>
