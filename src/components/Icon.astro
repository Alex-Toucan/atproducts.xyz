<!-- Icon.astro -->
<Fragment props>
  import { name, desc, href } from '@astro/props'

  // Define a function to render icons dynamically based on the 'name' prop
  function renderIcon(name) {
    // Here, you can use the 'name' prop to decide which icon to render
    // Return the appropriate icon component based on the 'name'
    return <YourIconComponent name={name} />;
  }
</Fragment>
